using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MenuHandelerScript : MonoBehaviour
{   
    [SerializeField] public Canvas MenuCanvas;
    [SerializeField] public GameObject MapSelectCanvas;
    [SerializeField] public GameObject SidebarCanvas;
    [SerializeField] public GameObject ObjectinfoCanvas;

    public bool SidebarActive = false;
    public bool ObjectInfoActive = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
